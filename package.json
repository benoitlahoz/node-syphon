{
  "name": "root",
  "private": true,
  "workspaces": [
    "lib/**/*",
    "examples/**/*"
  ],
  "scripts": {
    "build:all": "lerna run build",
    "update": "lernaupdate",
    "test": "lerna run test --"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "lerna-update-wizard": "^1.1.2"
  }
}
